# edges

Edge editing routes for one project.

Edges represent paper-to-paper relationships. User edits should only change editable descriptive fields and must preserve structural fields such as `id`, `source`, `target`, and `createdAt`.

Specific edge routes live in `[edgeId]/`.

